{
  "name": "nuthatch",
  "description": "A collection of game utilities.",
  "keywords": [
    "game",
    "engine"
  ],
  "scripts": {
    "publish": "npm run build && lerna publish --message \"🔖 Publish %s.\"",
    "build": "lerna bootstrap --hoist && lerna run build"
  },
  "author": "Danny Fritz <dannyfritz@gmail.com>",
  "license": "MIT",
  "devDependencies": {
    "lerna": "^2.4.0",
    "remark-lint": "^6.0.1",
    "remark-lint-checkbox-content-indent": "^1.0.1",
    "remark-preset-lint-recommended": "^3.0.1"
  }
}
